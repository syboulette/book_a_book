<h1>Book a Book!</h1>
<h2>I want to:</h2>
<p><%= link_to "Add a new Book!", "/books/new" %></p>
<p>Books are ready to be booked:</p>
<ul>
  <% @books.each do |book| %>
    <li><%= book.title %></li>
    <li><%= book.author %></li>
    <li><%= book.genre%></li>
    <p><%= link_to "Edit", "/books/:id/edit" %></p>
    <p><%= link_to "Show", "/books/:id" %></p>
  <% end %>
</ul>
